// A foreign `api` property must not pass as a peer, so both sides carry this.
export const API_MARKER = '8f82c8d5-0124-4ba3-befa-e8625f6c32b4';

// Bumped only when the contract changes, metadata.version moves on every upload.
export const API_VERSION = 1;

export const BETTER_PANEL_UUID = 'BetterPanel@nexaknight.com';
